Для того, щоб запустити проєкт, треба клонувати репозиторій. У терміналі git bash заходимо в потрібну теку, а далі завдяки команді git clone клонуємо репозиторій до себе.
Після цього треба відкрити редактор коду (який у вас є), перейти в потрібний проєкт, прописавши шлях до нього в консолі. Далі ввести команду npm install і почекати, коли
завантажиться тека node_modules. Щоб запустити застосунок треба мати на телефоні застосунок expo go, а в редакторі коду запустити сервер командою npx expo start --tunnel.
На екрані з'явиться qr код, який треба відсканувати

З приводу завдань: З фільтрацією у мене виникли проблемии, тому я її не доробив
